#include <stdio.h>
/**
 * main - print base sixteen
 * Return: 0 (successfully executed)
 */
int main(void)
{
	char basesixteen = '0';

	putchar(basesixteen);
	putchar(basesixteen + 1);
	putchar(basesixteen + 2);
	putchar('\n');
	return (0);
}
