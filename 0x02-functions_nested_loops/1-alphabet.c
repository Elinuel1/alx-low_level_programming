#include "main.h"
/**
 * main - print_alphabet llower cases
 * Return: 0 (successfully executed)
 */
int print_alphabet(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++);
	_putchar(lette);
	_putchar('\n');
	return (0);
}
