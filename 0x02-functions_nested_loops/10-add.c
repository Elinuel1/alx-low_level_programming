#include "main.h"
/**
 * add - addz teo integers and returns the result
 * @p: int to be added to q
 * @q: int to be added to p
 * Return: sum of p and q
 */
int add(int p, int q)
{
	int z;

	z = p + q;
	return (z);
}
